// Shared cookie names for student auth — safe to import from any context.
export const STUDENT_TOKEN_COOKIE = "student_at"
export const STUDENT_USER_COOKIE = "student_user"

// Must match backend JWT expiry
export const STUDENT_TOKEN_TTL_MINUTES = 15
